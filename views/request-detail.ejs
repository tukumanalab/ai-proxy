<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Request #<%= request.id %> - Tukumana Proxy</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: #0f172a;
      color: #e2e8f0;
      padding: 20px;
    }

    .container {
      max-width: 1400px;
      margin: 0 auto;
    }

    header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      padding: 30px;
      border-radius: 12px;
      margin-bottom: 30px;
      box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
    }

    .back-btn {
      display: inline-block;
      padding: 10px 20px;
      background: rgba(255, 255, 255, 0.1);
      color: white;
      text-decoration: none;
      border-radius: 8px;
      margin-bottom: 15px;
      backdrop-filter: blur(10px);
      transition: background 0.3s;
    }

    .back-btn:hover {
      background: rgba(255, 255, 255, 0.2);
    }

    h1 {
      font-size: 2em;
      margin-bottom: 10px;
    }

    .meta {
      display: flex;
      gap: 20px;
      flex-wrap: wrap;
      margin-top: 15px;
    }

    .meta-item {
      background: rgba(255, 255, 255, 0.1);
      padding: 10px 20px;
      border-radius: 8px;
      backdrop-filter: blur(10px);
    }

    .meta-label {
      font-size: 0.85em;
      opacity: 0.9;
    }

    .meta-value {
      font-size: 1.2em;
      font-weight: bold;
      margin-top: 5px;
    }

    .section {
      background: #1e293b;
      border-radius: 12px;
      padding: 25px;
      margin-bottom: 20px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
    }

    .section-title {
      font-size: 1.5em;
      margin-bottom: 15px;
      color: #a78bfa;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .code-block {
      background: #0f172a;
      border: 2px solid #334155;
      border-radius: 8px;
      padding: 20px;
      overflow-x: auto;
      font-family: 'Monaco', 'Courier New', monospace;
      font-size: 0.9em;
      line-height: 1.6;
    }

    .json-viewer {
      color: #e2e8f0;
    }

    .json-key {
      color: #a78bfa;
    }

    .json-string {
      color: #10b981;
    }

    .json-number {
      color: #f59e0b;
    }

    .json-boolean {
      color: #3b82f6;
    }

    .json-null {
      color: #ef4444;
    }

    .method {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 6px;
      font-weight: 600;
      font-size: 0.85em;
    }

    .method-GET { background: #10b981; color: white; }
    .method-POST { background: #3b82f6; color: white; }
    .method-PUT { background: #f59e0b; color: white; }
    .method-DELETE { background: #ef4444; color: white; }
    .method-PATCH { background: #8b5cf6; color: white; }

    .status {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 6px;
      font-weight: 600;
      font-size: 0.85em;
    }

    .status-2xx { background: #10b981; color: white; }
    .status-3xx { background: #3b82f6; color: white; }
    .status-4xx { background: #f59e0b; color: white; }
    .status-5xx { background: #ef4444; color: white; }

    .error-box {
      background: #7f1d1d;
      border: 2px solid #ef4444;
      border-radius: 8px;
      padding: 20px;
      color: #fca5a5;
      margin-top: 10px;
    }

    .copy-btn {
      float: right;
      padding: 8px 16px;
      background: #334155;
      color: #e2e8f0;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 0.9em;
      transition: background 0.3s;
    }

    .copy-btn:hover {
      background: #475569;
    }

    .path {
      font-family: 'Monaco', 'Courier New', monospace;
      color: #a78bfa;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 15px;
    }

    table th {
      text-align: left;
      padding: 10px;
      background: #334155;
      border-bottom: 2px solid #475569;
    }

    table td {
      padding: 10px;
      border-bottom: 1px solid #334155;
      word-break: break-all;
    }

    table tr:hover {
      background: #2d3748;
    }

    .logo-container {
      display: flex;
      align-items: center;
      gap: 20px;
      margin-bottom: 15px;
    }

    .logo {
      width: 60px;
      height: 60px;
      object-fit: contain;
    }

    .title-wrapper {
      flex: 1;
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <a href="/" class="back-btn">← Back to Dashboard</a>
      <div class="logo-container">
        <img src="/logo.png" alt="Tukumana Logo" class="logo">
        <div class="title-wrapper">
          <h1>Request #<%= request.id %></h1>
        </div>
      </div>
      <div class="meta">
        <div class="meta-item">
          <div class="meta-label">Method</div>
          <div class="meta-value"><span class="method method-<%= request.method %>"><%= request.method %></span></div>
        </div>
        <div class="meta-item">
          <div class="meta-label">Status</div>
          <div class="meta-value">
            <% if (request.statusCode) { %>
              <span class="status status-<%= Math.floor(request.statusCode / 100) %>xx"><%= request.statusCode %></span>
            <% } else if (request.error) { %>
              <span class="status status-5xx">ERROR</span>
            <% } else { %>
              <span class="status" style="background: #6b7280;">PENDING</span>
            <% } %>
          </div>
        </div>
        <div class="meta-item">
          <div class="meta-label">Duration</div>
          <div class="meta-value" style="color: #10b981;"><%= request.duration ? request.duration + 'ms' : '-' %></div>
        </div>
        <div class="meta-item">
          <div class="meta-label">Timestamp</div>
          <div class="meta-value" style="font-size: 0.9em;"><%= new Date(request.timestamp).toLocaleString('ja-JP') %></div>
        </div>
      </div>
    </header>

    <div class="section">
      <div class="section-title">📍 Request Details</div>
      <table>
        <tr>
          <th style="width: 150px;">Path</th>
          <td class="path"><%= request.path %></td>
        </tr>
        <tr>
          <th>Query</th>
          <td><pre class="code-block"><%= request.query || '{}' %></pre></td>
        </tr>
      </table>
    </div>

    <div class="section">
      <div class="section-title">
        📤 Request Headers
        <button class="copy-btn" onclick="copyToClipboard('request-headers')">Copy</button>
      </div>
      <pre id="request-headers" class="code-block"><%= formatJSON(request.headers) %></pre>
    </div>

    <% if (request.requestBody) { %>
      <div class="section">
        <div class="section-title">
          📝 Request Body
          <button class="copy-btn" onclick="copyToClipboard('request-body')">Copy</button>
        </div>
        <pre id="request-body" class="code-block"><%= formatJSON(request.requestBody) %></pre>
      </div>
    <% } %>

    <%
      const isBlocked = request.error && request.error.includes('NG word');
    %>
    <% if (isBlocked) { %>
      <div class="section">
        <div class="section-title">🚫 Request Blocked</div>
        <div class="error-box" style="background: #7f1d1d; border-color: #dc2626;">
          <strong>⚠️ This request was blocked by NG word filter</strong><br><br>
          <strong>Reason:</strong> <%= request.error %><br><br>
          <%
            try {
              const responseBody = JSON.parse(request.responseBody || '{}');
              if (responseBody.blocked_word) {
          %>
          <strong>Blocked Word:</strong> <code style="background: rgba(255,255,255,0.1); padding: 2px 8px; border-radius: 4px;"><%= responseBody.blocked_word %></code>
          <%
              }
            } catch (e) {}
          %>
        </div>
      </div>
    <% } else if (request.error) { %>
      <div class="section">
        <div class="section-title">❌ Error</div>
        <div class="error-box">
          <strong>Error Message:</strong><br>
          <%= request.error %>
        </div>
      </div>
    <% } %>

    <% if (request.responseHeaders) { %>
      <div class="section">
        <div class="section-title">
          📥 Response Headers
          <button class="copy-btn" onclick="copyToClipboard('response-headers')">Copy</button>
        </div>
        <pre id="response-headers" class="code-block"><%= formatJSON(request.responseHeaders) %></pre>
      </div>
    <% } %>

    <% if (request.responseBody) { %>
      <div class="section">
        <div class="section-title">
          📦 Response Body
          <button class="copy-btn" onclick="copyToClipboard('response-body')">Copy</button>
        </div>
        <pre id="response-body" class="code-block"><%= formatJSON(request.responseBody) %></pre>
      </div>
    <% } %>
  </div>

  <script>
    function copyToClipboard(elementId) {
      const element = document.getElementById(elementId);
      const text = element.textContent;
      navigator.clipboard.writeText(text).then(() => {
        alert('Copied to clipboard!');
      });
    }
  </script>

  <%
    function formatJSON(str) {
      try {
        const obj = typeof str === 'string' ? JSON.parse(str) : str;
        return JSON.stringify(obj, null, 2);
      } catch (e) {
        return str;
      }
    }
  %>
</body>
</html>
