<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Request #<%= request.id %> - AI Proxy</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: #f5f5f5;
      color: #333;
      padding: 20px;
    }

    .container {
      max-width: 1400px;
      margin: 0 auto;
    }

    header {
      background: #fff;
      padding: 20px 30px;
      border-radius: 8px;
      margin-bottom: 20px;
      border: 1px solid #e0e0e0;
    }

    .back-btn {
      display: inline-block;
      padding: 8px 16px;
      background: #f5f5f5;
      color: #333;
      text-decoration: none;
      border-radius: 4px;
      margin-bottom: 15px;
      border: 1px solid #ddd;
      font-size: 0.9em;
    }

    .back-btn:hover {
      background: #eee;
    }

    h1 {
      font-size: 1.5em;
      font-weight: 600;
      color: #333;
      margin-bottom: 15px;
    }

    .meta {
      display: flex;
      gap: 30px;
      flex-wrap: wrap;
    }

    .meta-item {
      display: flex;
      align-items: baseline;
      gap: 8px;
    }

    .meta-label {
      font-size: 0.9em;
      color: #666;
    }

    .meta-value {
      font-size: 1em;
      font-weight: 500;
      color: #333;
    }

    .section {
      background: #fff;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 15px;
      border: 1px solid #e0e0e0;
    }

    .section-title {
      font-size: 1.1em;
      font-weight: 600;
      margin-bottom: 15px;
      color: #333;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .code-block {
      background: #fafafa;
      border: 1px solid #e0e0e0;
      border-radius: 4px;
      padding: 15px;
      overflow-x: auto;
      font-family: 'Monaco', 'Courier New', monospace;
      font-size: 0.85em;
      line-height: 1.6;
      color: #333;
    }

    .method {
      display: inline-block;
      padding: 3px 8px;
      border-radius: 3px;
      font-weight: 500;
      font-size: 0.8em;
    }

    .method-GET { background: #e8f5e9; color: #2e7d32; }
    .method-POST { background: #e3f2fd; color: #1565c0; }
    .method-PUT { background: #fff3e0; color: #ef6c00; }
    .method-DELETE { background: #ffebee; color: #c62828; }
    .method-PATCH { background: #f3e5f5; color: #7b1fa2; }

    .status {
      display: inline-block;
      padding: 3px 8px;
      border-radius: 3px;
      font-weight: 500;
      font-size: 0.8em;
    }

    .status-2xx { background: #e8f5e9; color: #2e7d32; }
    .status-3xx { background: #e3f2fd; color: #1565c0; }
    .status-4xx { background: #fff3e0; color: #ef6c00; }
    .status-5xx { background: #ffebee; color: #c62828; }

    .error-box {
      background: #fff5f5;
      border: 1px solid #ffcdd2;
      border-radius: 4px;
      padding: 15px;
      color: #c62828;
      margin-top: 10px;
    }

    .copy-btn {
      padding: 6px 12px;
      background: #f5f5f5;
      color: #333;
      border: 1px solid #ddd;
      border-radius: 4px;
      cursor: pointer;
      font-size: 0.85em;
    }

    .copy-btn:hover {
      background: #eee;
    }

    .path {
      font-family: 'Monaco', 'Courier New', monospace;
      color: #555;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    table th {
      text-align: left;
      padding: 10px;
      background: #fafafa;
      border-bottom: 1px solid #e0e0e0;
      font-weight: 600;
      color: #666;
    }

    table td {
      padding: 10px;
      border-bottom: 1px solid #f0f0f0;
      word-break: break-all;
    }

    .duration {
      color: #2e7d32;
      font-weight: 500;
    }

    .timestamp {
      color: #888;
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <a href="/" class="back-btn">← Back to Dashboard</a>
      <h1>Request #<%= request.id %></h1>
      <div class="meta">
        <div class="meta-item">
          <span class="meta-label">Method:</span>
          <span class="meta-value"><span class="method method-<%= request.method %>"><%= request.method %></span></span>
        </div>
        <div class="meta-item">
          <span class="meta-label">Status:</span>
          <span class="meta-value">
            <% if (request.statusCode) { %>
              <span class="status status-<%= Math.floor(request.statusCode / 100) %>xx"><%= request.statusCode %></span>
            <% } else if (request.error) { %>
              <span class="status status-5xx">ERROR</span>
            <% } else { %>
              <span class="status" style="background: #f5f5f5; color: #666;">PENDING</span>
            <% } %>
          </span>
        </div>
        <div class="meta-item">
          <span class="meta-label">Duration:</span>
          <span class="meta-value duration"><%= request.duration ? request.duration + 'ms' : '-' %></span>
        </div>
        <div class="meta-item">
          <span class="meta-label">Time:</span>
          <span class="meta-value timestamp"><%= new Date(request.timestamp).toLocaleString('ja-JP') %></span>
        </div>
      </div>
    </header>

    <div class="section">
      <div class="section-title">Request Details</div>
      <table>
        <tr>
          <th style="width: 120px;">Path</th>
          <td class="path"><%= request.path %></td>
        </tr>
        <tr>
          <th>Query</th>
          <td><pre class="code-block"><%= request.query || '{}' %></pre></td>
        </tr>
      </table>
    </div>

    <div class="section">
      <div class="section-title">
        Request Headers
        <button class="copy-btn" onclick="copyToClipboard('request-headers')">Copy</button>
      </div>
      <pre id="request-headers" class="code-block"><%= formatJSON(request.headers) %></pre>
    </div>

    <% if (request.requestBody) { %>
      <div class="section">
        <div class="section-title">
          Request Body
          <button class="copy-btn" onclick="copyToClipboard('request-body')">Copy</button>
        </div>
        <pre id="request-body" class="code-block"><%= formatJSON(request.requestBody) %></pre>
      </div>
    <% } %>

    <%
      const isBlocked = request.error && request.error.includes('NG word');
    %>
    <% if (isBlocked) { %>
      <div class="section">
        <div class="section-title">Request Blocked</div>
        <div class="error-box">
          <strong>This request was blocked by NG word filter</strong><br><br>
          <strong>Reason:</strong> <%= request.error %><br><br>
          <%
            try {
              const responseBody = JSON.parse(request.responseBody || '{}');
              if (responseBody.blocked_word) {
          %>
          <strong>Blocked Word:</strong> <code style="background: #ffebee; padding: 2px 8px; border-radius: 4px;"><%= responseBody.blocked_word %></code>
          <%
              }
            } catch (e) {}
          %>
        </div>
      </div>
    <% } else if (request.error) { %>
      <div class="section">
        <div class="section-title">Error</div>
        <div class="error-box">
          <strong>Error Message:</strong><br>
          <%= request.error %>
        </div>
      </div>
    <% } %>

    <% if (request.responseHeaders) { %>
      <div class="section">
        <div class="section-title">
          Response Headers
          <button class="copy-btn" onclick="copyToClipboard('response-headers')">Copy</button>
        </div>
        <pre id="response-headers" class="code-block"><%= formatJSON(request.responseHeaders) %></pre>
      </div>
    <% } %>

    <% if (request.responseBody) { %>
      <div class="section">
        <div class="section-title">
          Response Body
          <button class="copy-btn" onclick="copyToClipboard('response-body')">Copy</button>
        </div>
        <pre id="response-body" class="code-block"><%= formatJSON(request.responseBody) %></pre>
      </div>
    <% } %>
  </div>

  <script>
    function copyToClipboard(elementId) {
      const element = document.getElementById(elementId);
      const text = element.textContent;
      navigator.clipboard.writeText(text).then(() => {
        alert('Copied to clipboard!');
      });
    }
  </script>

  <%
    function formatJSON(str) {
      try {
        const obj = typeof str === 'string' ? JSON.parse(str) : str;
        return JSON.stringify(obj, null, 2);
      } catch (e) {
        return str;
      }
    }
  %>
</body>
</html>
